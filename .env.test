APP_ENV=test
APP_DEBUG=0
APP_SECRET=test-secret
CONTEXT_LOOM_AUTH=apikey
CONTEXT_LOOM_API_KEY=test-api-key-12345
# Host allowlist for the DNS-rebinding regression tests (McpFlowTest):
# localhost keeps the dev-style requests flowing, loom.example.com proves a
# public hostname passes when listed (and evil.example.net is 403'd).
CONTEXT_LOOM_ALLOWED_HOSTS=localhost,loom.example.com
